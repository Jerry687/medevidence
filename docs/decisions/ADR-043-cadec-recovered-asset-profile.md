# ADR-043: CADEC recovered local asset profile

Status: local development recovery profile, data freeze reviewed 2026-09-18.

The original CADEC archive is available again at `D:/Projects/medevidence/.local/data/cadec-approved-20260918/CADEC.v2.zip`. Its 1,870,497 bytes have the original SHA-256 `4045b926a0a5735f00f785f7ad935e5a73731d6ab607d11d88880a334be18c4a`. The original 1,699,979-byte external manifest with SHA-256 `1c475ded0e7a2e0d80fe0909f2ccf1131c746da6ffc9c52879bfd9076234abfa` was not recovered. Its identity remains historical and is never assigned to reconstructed bytes.

The named successor is the external metadata-only `manifest-recovery-v1.json`, 1,235,699 bytes, SHA-256 `a450e571db19c8d2c79944363daea5421e9aad8101e3da7f14e9cc464360c75b`. Its distinct namespaced hash is the recovery corpus version. The reviewed data audit is `recovery-audit-v1-reviewed.json`, 1,909 bytes, SHA-256 `4a9c87b32e739926098a570ed51d82543393bcd2e54b3683992cde7470dd4dc7`. That audit records an independent **data freeze** check; it does not certify runtime behavior, clinical use, or the full release. The earlier 320-byte pending note remains separate and is not the active audit. The original freeze audit hash remains bound only to the original manifest profile.

The successor retains the original approved subset and policy: 1,250 canonical documents, the same two exact complete-document exclusions (`DICLOFENAC-SODIUM.7`, `LIPITOR.221`), 1,248 admitted documents, train/development/test counts 992/119/137 and original membership hashes, five malformed rows rejected, 2/44/45 visible reference-binding limitations, the sole SHA-bound Windows-1252 member, and 9,089/6,300/9,089 provider-gold annotation rows. It does not repair, normalize, or reinterpret annotation text. Only high-level MedDRA and SNOMED CT references are retained; no terminology payload is emitted. CADEC remains auxiliary-only and cannot support clinical, causal, incidence, product-risk, ranking, or advice claims.

The loader admits exactly either the old immutable manifest identity or this successor identity and verifies every ZIP member from the exact archive before producing metadata. Release, child, locator, plan, verification, and query identities bind the selected profile as one closed tuple; mixed old/new identities reject. The old profile remains the default for existing callers. The restored asset remains under project `.local/` and is never copied into Git, model input, or persisted report text. Local search retains transient text only while scoring, returns document references, and persists only metadata receipts.

The first real 20-reference local composition attempt failed before receipt publication: its canonical metadata receipt measured 69,624 bytes, above the previous 65,536-byte cap. The receipt read/write cap is now 131,072 bytes. The same bounded read checks apply on replay and index lookup; a 20-reference synthetic regression passes and 131,073-byte receipt input rejects. The failed attempt remains a separate immutable record.

Repeated CADEC provenance checks on the same runtime previously reparsed the full archive for each reference. The local adapter now holds at most eight immutable, size-bounded, serialized **metadata-only** search results keyed by the exact plan and scope. Every cached return still reads both asset files under the existing finite byte bounds and recomputes their exact SHA-256 identities before reconstructing the typed result. A cold local search measured 37.134 seconds; an immediate verified-cache search of the same plan measured 0.003 seconds. These are local observations, not a guarantee for a cold start or another machine. No archive bytes or text enter the cache.

The distinct real local composition `run-003` completed with 20/20 reference-only materials and generic provenance lookups; code, asset, and snapshot file hashes were rechecked. Collection and first terminal replay were cold at 32.086 and 32.669 seconds; subsequent material read was 0.040 seconds and 20 provenance lookups totaled 0.526 seconds. The raw result SHA-256 is `32b512d84ac073dd8e1f1d34233ccecd0ec34a31b5d1154841aa13e1812b0b44`. Earlier failed and aborted attempts remain preserved separately. This local run does not prove production readiness or clinical suitability.

No live medical-source access, dependency change, raw corpus fixture, provider licence expansion, or production acceptance is implied by this recovery.
