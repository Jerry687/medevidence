# M1B-DM-003 delivery record

## Status

`TERMINAL_REAUDIT002_PASS_AWAITING_FINAL_BYTE_REBIND_AND_GIT`.
Independent Review 002 historically returned
`FAIL — P0 0 / P1 1 / P2 0` on exact 23-path manifest
`242b5442db3d6f0c9f43d4c45a05f221f627cc3811bc95b95d7472fd2530b789`.
The Owner authorized exactly one additional mechanical cycle for that sole
requiredness defect. Independent Review 003 returned historical
`FAIL — P0 0 / P1 1 / P2 0` because enabled generation strengthened three
shared PubMed components. Same-class remediation and fresh offline validation
are complete. Independent Review 004 returned
`PASS — P0 0 / P1 0 / P2 0` on reviewed canonical manifest
`d149a36a369591963010456664999bb07c114041972ed9b104ff059370875c4a`.
Terminal Audit 001 returned `FAIL — P0 0 / P1 0 / P2 1` because the authorized
DM-003 traceability section remained stale. That metadata-only finding is now
remediated. Review 005 then returned `FAIL — P0 0 / P1 0 / P2 1` on a stale
required-nullable security sentence. That documentation-only finding is now
remediated. Review 006 returned `PASS — P0 0 / P1 0 / P2 0` on exact reviewed
manifest `391abc4fc5b8e999295b7468812e6b76ad2aa2da9b85b0e2c46ec11d494f6ded`.
Terminal Re-Audit 002 returned `PASS — P0 0 / P1 0 / P2 0` on audited
pre-persistence manifest
`a7ecad26899a2ed1ce46b53e9d839e69459b8e81d946cf6aba4296557f7a0830`.
Persisting that PASS changes evidence bytes, so final-byte rebind and any
separately authorized Git lifecycle remain pending. The work item is not
complete or integrated; live-source and FAERS work have not run.

## Identity and authority

- Branch: `feat/m1b-dm-003-report-api`
- Baseline: `2c235ecfc9d565de14a74b0b86aadd8f63b6a1e6`
- Freeze SHA-256:
  `e44778f42585134634e2311e16d61d2269f3da06f79ac74c8ed37cbaa701ea70`
- Runtime/validation permitted hosts: none
- Medical-source network execution: not authorized and not performed
- Production dependencies, schema, migration, connector, ingestion, and
  persistence changes: none

## Behavior

- `build_dailymed_report` builds and revalidates the closed
  `M1BResearchReportV1` from exact trusted evidence tuples.
- Planning is trusted: exactly one selected DailyMed entry is constructed by
  the tool, with no caller planning fields or skip reasons.
- `POST /v1/research/dailymed` is additive, typed, discriminator-closed, and
  returns only a research-only draft with `exportable=false`.
- Existing PubMed route registration remains unchanged when the optional
  DailyMed application callable is absent. Its complete default OpenAPI bytes
  are pinned to the previous M1A fixture identity, contain no M1B advertisement,
  and both default and enabled graphs recursively resolve every local ref.
- DailyMed application responses require every serialized report field and all
  nested model fields before Pydantic defaults; omissions fail closed as
  `502 tool_contract_error`.
- DailyMed OpenAPI documents only its producible error surface; a non-DailyMed
  source request is the generic `invalid_request` at `/requested_sources`.
- The live DailyMed smoke module is disabled by default and requires a separate
  exact one-run Owner authorization.

## Validation evidence

Fresh local evidence on the implementation candidate:

- Focused report/API/OpenAPI with `--disable-socket`: `96 passed`, one
  pre-existing FastAPI TestClient deprecation warning.
- Offline DailyMed route integration with `--disable-socket`: `2 passed`.
- Disabled live harness collection with `--disable-socket`: `1 skipped` for
  missing separate exact Owner authorization; no live request ran.
- Repository Ruff: PASS; repository format check: PASS (`83 files`).
- Strict MyPy: PASS (`40 source files`).
- Full offline unit/contract gate: `1234 passed`, two expected warnings,
  `79%` total coverage, and `coverage.xml` generated without a tracked diff.
- `git diff --check`: PASS.
- Exact path-scope audit: PASS; all 23 changed paths are authorized and no
  denied or unexpected path changed.

The first integration attempt used the Windows Proactor event loop, whose
internal self-pipe was correctly blocked by pytest-socket. Mechanical
remediation cycle 1 replaced it with direct execution of the actual FastAPI
route coroutine over an immediate in-memory ASGI `Request`; the final
integration gate passes while sockets remain disabled.

Mechanical remediation cycle 2 made required M1B request/response discriminator
fields explicit in OpenAPI and required top-level and DailyMed request-element
discriminators at the raw request boundary. Focused, static, and full offline
gates passed afterward.

Final authorized remediation cycle 3 closed all four Independent Review 001
findings: exact disabled-app M1A OpenAPI identity and two-configuration ref
closure; strict pre-default response presence; the producible DailyMed error
catalog/path; and the full frozen report-state/trusted-evidence plus
degraded/failed/stable route matrix. The automatic remediation limit was
exhausted at that historical point; the subsequent narrow Owner authorization
is recorded below.

Independent Review 002 verified the Review 001 closures but reproduced one new
P1 contract defect: enabled OpenAPI requiredness is narrower than runtime
strict presence for nested `SourceOutcome`, `RetainedSplResponse`,
`LabelSection`, `DailyMedLabelVersion`, `DailyMedLocatorV1`, `DomainWarning`,
and `ResearchScope` fields. It requires exact default-M1A byte preservation,
complete enabled nested requiredness including all 44 locator fields, expanded
negative contract tests, preserved `502` omission behavior, and fixture/
manifest rebind. Those criteria are not new implementation authorization.

The Owner then authorized exactly one additional mechanical remediation cycle.
The enabled response graph now derives strict requiredness recursively from the
actual model-generated property inventory. Response-only components are updated
in place. Three models shared by request and response receive deterministic
response-specific components so request-runtime-optional fields remain optional
on input while the response requires their serialized presence. Required
nullable fields remain null-capable. `DailyMedLocatorV1` is exactly `44/44`
required. Mapping and `model_construct` omission tests cover all 11 formerly
divergent response models and all 44 locator fields. The default disabled
OpenAPI remains byte-identical at SHA-256
`0d735acbbb1503dcc3235a37193b9d383cae08b8dc4fdb3b0e42616982ff028a`;
the regenerated enabled fixture SHA-256 is
`2a62d84a010729c3a1efefc292cc643f27c85c636163e181b4eb7dfc3ec2de61`.
That candidate was submitted to Independent Review 003; terminal audit remained
pending.

Independent Review 003 bound canonical manifest
`eeb5d0ffbfd28e6d64b9c20edce0065bd1b63e2857bf4fc25f0c4c7bd5593d8e`
and returned `FAIL — P0 0 / P1 1 / P2 0`. The response-strengthening pass
mutated shared `DomainWarning` and `SourceOutcome` required sets and reordered
`ExecutionBounds.required`, changing the enabled PubMed 76-component subtree
from frozen SHA-256
`985edba15e6f6dda1aa5339a81b7de79144dd13d39328e1b6c4dc331e0e8994e`
to `f38e24c7ea8feda0b3c9c7af84e5c13ec9380af8f3597bf5658762b988344803`.

The Owner-authorized same-cycle remediation now protects the complete
PubMed-reachable component graph generically. Any component that needs stronger
M1B response presence and is shared with PubMed or M1B input is cloned under a
deterministic response name; only M1B response references are rewritten.
Already exact shared components are untouched, including required-field order.
The enabled and disabled PubMed path plus all 76 transitive components now
compare byte-for-byte equal. All prior M1B response parity, input optionality,
nullability, locator-44, omission, and local-reference tests remain green. The
regenerated enabled fixture SHA-256 is
`339ba5cce753ae7b2201350ee70135c316ae7cfb99845de28ad10f94ae1b566e`.
At that checkpoint, Independent Review 004 and terminal audit remained pending.

Independent Review 004 independently verified the complete closure and returned
`PASS — P0 0 / P1 0 / P2 0`. It bound canonical manifest
`d149a36a369591963010456664999bb07c114041972ed9b104ff059370875c4a`
using ordinal repository-relative POSIX path, tab, byte count, tab, lowercase
raw-file SHA-256 rows encoded as UTF-8 without BOM, LF-separated, with no
terminal LF. The implementer checkpoint manifest
`2ffd5f0bdf13c061add9000ca7c96e4f201b72300807d546778ff0fa8feaaf65`
is superseded by that reviewed identity.

Review 004 verified the frozen PubMed subtree SHA-256
`985edba15e6f6dda1aa5339a81b7de79144dd13d39328e1b6c4dc331e0e8994e`,
default OpenAPI SHA-256
`0d735acbbb1503dcc3235a37193b9d383cae08b8dc4fdb3b0e42616982ff028a`,
and enabled fixture SHA-256
`339ba5cce753ae7b2201350ee70135c316ae7cfb99845de28ad10f94ae1b566e`.
Executable evidence was focused `194 passed`, omission `60 passed`, integration
`2 passed`, live harness `1 skipped`, boundary/offline `12 passed`, Ruff PASS,
format PASS (`83 files`), MyPy PASS (`40 source files`), full offline `1234
passed` with two expected warnings and `79%` coverage, and offline lock
resolution `62 packages`. Scope was exactly `23` authorized paths with an empty
index and zero unexpected paths. Network, Docker, database, and Git mutation
were all absent.

Historical Reviews 001-003 remain FAIL evidence. Review 004 PASS does not
replace the required terminal audit and does not authorize a completion,
commit, push, PR, CI, merge, integration, live-source, or FAERS claim.

Terminal Audit 001 bound exact canonical `StringComparer.Ordinal` manifest
`23748ca9d4db441cc79a14da90c2ad18f8b62bd4f5e96ae77a0b2cab5df3447c`
and returned `FAIL — P0 0 / P1 0 / P2 1`. The case-insensitive alias
`3c9a362a0cb7abb4930d2c4d6d2f78377fe444b9dcabd8f70db62209a7d05d47`
covered the same bytes but is superseded and noncanonical. The sole finding was
stale `docs/TRACEABILITY_MATRIX.md` metadata that still stopped at Review 001.
The authorized DM-003 traceability section now preserves Review 001
`FAIL — P0 0 / P1 2 / P2 2`, Review 002 `FAIL — P0 0 / P1 1 / P2 0`, Review
003 `FAIL — P0 0 / P1 1 / P2 0`, Review 004
`PASS — P0 0 / P1 0 / P2 0`, their identities, and Audit 001's failed terminal
gate. No implementation behavior changed. Fresh metadata Review 005 and
terminal re-audit remain required.

Audit 001 execution evidence was focused `127 passed`, omission `60 passed`,
integration `2 passed`, disabled live harness `1 skipped`, boundary/offline
`12 passed`, Ruff PASS, format PASS (`83 files`), MyPy PASS (`40 source files`),
full offline `1234 passed` with two expected warnings and `79%` coverage, and
offline lock resolution `62 packages`. It reverified default OpenAPI SHA-256
`0d735acbbb1503dcc3235a37193b9d383cae08b8dc4fdb3b0e42616982ff028a`,
enabled fixture SHA-256
`339ba5cce753ae7b2201350ee70135c316ae7cfb99845de28ad10f94ae1b566e`,
exact equality of the PubMed path and all `76` transitive components at frozen
subtree SHA-256
`985edba15e6f6dda1aa5339a81b7de79144dd13d39328e1b6c4dc331e0e8994e`,
and locator requiredness `44/44`. UTF-8/BOM/CR counts were zero and diff check
passed. No dependency, migration, connector, ingestion, persistence, FAERS,
Docker, database, Git, medical-source, or other network operation occurred.

Independent Review 005 bound canonical `StringComparer.Ordinal` manifest
`17eeaea2c32c86b5766251d21c7ed8e0824ce68a7f745497333349fa0fcedafd`
and returned `FAIL — P0 0 / P1 0 / P2 1`. Its case-insensitive alias
`5a8ae15acd7cb46cc37f013f4f458d9260c190b5fc3df69785923a4c2e6800ff`
is noncanonical. The sole finding was that `docs/SECURITY.md` described a
selected DailyMed plan entry as having “no reason fields,” although the exact
runtime/OpenAPI response requires `reason_code=None` and `reason=None` to be
present. Review evidence was focused `127 passed`, integration `2 passed`, live
harness `1 skipped`, boundary/offline `12 passed`, Ruff/format/MyPy PASS, and
full offline `1234 passed` with two expected warnings and `79%` coverage.

The authorized successor changes only the exact DM-003 security sentence:
selected plan entries retain required `reason_code` and `reason` fields with
null values and contain no skip-reason values. No code, test, fixture, runtime,
OpenAPI, dependency, route, domain, connector, parser, persistence, migration,
FAERS, network, Git, Docker, or database behavior changed. At that checkpoint,
Review 006 and terminal re-audit remained required.

Independent Review 006 bound exact canonical `StringComparer.Ordinal` manifest
`391abc4fc5b8e999295b7468812e6b76ad2aa2da9b85b0e2c46ec11d494f6ded`
and returned `PASS — P0 0 / P1 0 / P2 0`. It verified the SECURITY closure,
evidence hashes, exact 23-path scope, empty index, and unchanged implementation
behavior. Evidence was focused `127 passed`, integration `2 passed`, disabled
live harness `1 skipped`, boundary/offline `14 passed`, Ruff PASS, format PASS
(`83 files`), MyPy PASS (`40 source files`), full offline `1234 passed` with two
expected warnings and `79%` coverage, and offline lock resolution `62
packages`. It also reverified default OpenAPI SHA-256
`0d735acbbb1503dcc3235a37193b9d383cae08b8dc4fdb3b0e42616982ff028a`,
enabled fixture SHA-256
`339ba5cce753ae7b2201350ee70135c316ae7cfb99845de28ad10f94ae1b566e`,
and exact equality of the PubMed path plus all `76` transitive components at
frozen subtree SHA-256
`985edba15e6f6dda1aa5339a81b7de79144dd13d39328e1b6c4dc331e0e8994e`.
Diff, encoding, scope, dependency, and evidence audits passed. No network, Git,
Docker, database, dependency, migration, connector, ingestion, persistence, or
FAERS operation/change occurred. At that checkpoint, terminal re-audit remained
required.

Terminal Re-Audit 002 independently bound exact canonical
`StringComparer.Ordinal` manifest
`a7ecad26899a2ed1ce46b53e9d839e69459b8e81d946cf6aba4296557f7a0830`
and returned `PASS — P0 0 / P1 0 / P2 0`, with equal start/end identity, empty
index, and zero unexpected paths. It verified the four persistence-ready
evidence hashes, focused `127 passed` with one warning, integration `2 passed`,
live harness `1 skipped`, boundary/offline `14 passed` with one warning,
Ruff/format (`83 files`)/MyPy (`40 source files`) PASS, full offline `1234
passed` with two warnings and `79%` coverage, lock resolution `62 packages`,
default/enabled/PubMed graph hashes, locator `44/44`, and required-nullable
reason fields. Encoding and diff audits passed. No network, Docker, database,
dependency, Git, live-source, or FAERS operation occurred. This PASS is not a
commit, integration, or completion claim; final-byte rebind is pending.

## Network and Git operations

- DailyMed, NCBI, PubMed, FAERS, CADEC, and other medical-source requests: none.
- Other network operations: none.
- Git operations: read-only status/diff/identity inspection only; no stage,
  commit, push, merge, rebase, reset, clean, or remote-state operation.

## Remaining risk and manual verification

The implementation is not ready for Git lifecycle or delivery until the
post-persistence evidence bytes receive final-byte rebind. The additive route
also requires an explicitly injected trusted DailyMed application callable; no
live/default transport exists.

The Owner should be able to answer:

1. Why must the report tool, rather than the caller, construct the DailyMed plan?
2. Which exact discovery/fetch conditions permit stable label text and a locator?
3. Why does the disabled live harness provide no live-source acceptance evidence?
