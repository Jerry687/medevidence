# M1B-DM-001 delivery candidate

## Status

`TERMINAL_AUDIT_001_PASS_AWAITING_FINAL_BYTE_REBIND`

The first complete independent review failed with `P0/P1/P2 = 0/4/1`. After
final bounded remediation 3/3 and fresh offline validation, the fresh
independent review also failed with `P0/P1/P2 = 0/2/0`. The Owner then
authorized one extra remediation cycle limited to those two P1 findings. Both
implementation closures and fresh offline validation are complete; fresh
independent Review 002 returned `FAIL — P0 0 / P1 1 / P2 1`. The remaining P1
is same-class P1-02 mechanical binding work within the Owner-authorized extra
cycle 1/1, and the P2 is its mechanically dependent mandatory evidence.
Same-class remediation and fresh validation completed, but Review 003 returned
`FAIL — P0 0 / P1 2 / P2 0`. The Owner-authorized extra cycle 1/1 is consumed.
At that checkpoint, status was `OWNER_DECISION_REQUIRED` for another bounded
mechanical remediation cycle, and terminal audit plus all Git/integration steps
were prohibited.

The Owner subsequently authorized exactly one new cycle limited to Review 003
P1-01 and P1-02. Both implementation closures and fresh offline validation are
complete, but Review 004 returned `FAIL — P0 0 / P1 1 / P2 0`. The finding is
same-class public-binding work covered by the Owner's explicit do-not-stop
clause. Batch mechanical remediation and fresh validation are complete; Review
005 returned the failure recorded below. This record does not claim PASS or
authorize Git, network, integration, or `M1B-DM-002`.

Review 005 returned `FAIL — P0 0 / P1 2 / P2 0`. It verified the Review 004
closures, then found incomplete discovery/candidate-set authority in decisions
and incomplete duplicated locator identity comparisons. Same Owner-authorized
same-class batch remediation and fresh validation completed before the Review
006 failure recorded below.

Post-Review 005 remediation is complete. Decision validation now binds the
exact authoritative candidate discovery tuple, recomputes positive- and
zero-candidate `candidate_set_id`, and requires trusted source-outcome identity
plus discovery-manifest hash context. Report revalidation covers selected
failed-fetch, review-required, and no-candidate paths. Locator optional decision
and retained-response comparators now compare every duplicated identity field.
No schema or public concept was added. Review 006 returned
`FAIL — P0 0 / P1 1 / P2 0` on manifest
`75416c4fb6a3df9bbcf40783bcc4aab9f12e3d3c8df9118fdaebfe7f756dbeef`.
It accepted cross-request reuse of `acquisition_id`, `snapshot_id`, and
`source_outcome_id` because only source/query disjointness and ordinal
uniqueness are global. This is an Owner-authorized same-class exact-binding
defect. Post-Review 006 remediation now requires global uniqueness of those
three identities across the trusted report collection while preserving valid
reuse of `acquisition_intent_id`. Fresh root validation passes. Review 007 and
terminal audit were the next gates. Review 007 returned
`FAIL — P0 0 / P1 1 / P2 0` on manifest
`9a1ad8c2d2850c2b5ffcff67d5e19017beee740eb183c6b222270d1bdee258ca`.
It found that publicly exported `DailyMedTrustPath` rejects malformed rows only
inside the parent policy but accepts them at its standalone `model_validate`
boundary. Same Owner-authorized security P1-01 remediation is in progress;
the standalone model now admits exactly one of the six frozen rows, with direct
negative coverage for path, purpose, query, exact-query, omission, duplication,
and reordering drift. Parent-policy validation is unchanged, and no schema or
public concept changed. Fresh root validation passes; Review 008 and terminal
audit were the next gates. Review 008 returned `FAIL — P0 0 / P1 2 / P2 0`
on manifest
`939e99998c63dfe3ae664aa5ef6e265bc28e0e2787ea7cb73a32002dfb29e93e`.
It found existing-instance revalidation bypasses across exported security
policy classes and standalone/mutated-instance drift in the exact four-row
LOINC oracle. Both are mechanical manifestations of already frozen
non-weakenability and LOINC semantics. Remediation is in progress; Review 009
and terminal audit were the next gates. Post-Review 008 remediation now
revalidates existing instances for all six exported security models, including
nested connector-policy instances, and rejects drift. Standalone LOINC rows
admit exactly one of four frozen rows, while row and oracle instances receive
complete revalidation. Frozen values remain unchanged. Fresh root validation
passes; Review 009 and terminal audit are pending.

## Identity and authority

- Branch: `feat/m1b-dm-001-contract-freeze`
- Approved baseline: `ebcd11eb91aa02ae9a7115188ea10604e9f335d1`
- Owner freeze:
  `M1B-OWNER-PLANNING-FREEZE-v7-owner-resolution-final-r1.md`
- Freeze SHA-256:
  `e44778f42585134634e2311e16d61d2269f3da06f79ac74c8ed37cbaa701ea70`
- Candidate commit: pending
- Work item: `M1B-DM-001`
- Next work item `M1B-DM-002`: not authorized or started

## What changed

The candidate adds source-neutral, typed DailyMed contracts without changing
the default M1A wire behavior:

- exact candidate, decision, label-version, and section models;
- the exhaustive DailyMed discovery/selection matrix, including complete-only
  authoritative selection and review-required partial matches;
- canonical non-nil lowercase SETID and positive canonical SPL-version rules;
- the exact LOINC 2.82 four-section registry;
- additive M1B request, report, source-section, acquisition-reference, and
  locator contracts;
- locator admission only after a selected decision and successful, complete,
  usable fetch;
- explicit discovery-only degraded sections;
- non-authorizing connector trust metadata, empty ordinary validation hosts,
  and typed ZIP/XML security-contract metadata only.

Final bounded remediation 3/3 added the closed `unknown` marketing state; fixed
the label-version identity to its exact five-field preimage; added and exported
`RetainedSplResponse` and `LabelSelectionWarning`; made candidate ordering and
meaningful-dimension equivalence derived and canonical; closed locator,
retained-response, requested-section, and report cardinality bindings; and
expanded the exact LOINC, connector trust, transport/cache, XML, and ZIP typed
oracles. All changes are contract metadata and deterministic validation only.

Owner-authorized extra remediation 1/1 closed only Review 001 findings P1-01
and P1-02. Frozen security-policy tuple and nested-policy values now reject
caller weakening, reordering, duplication, omission, or drift. DailyMed search
and fetch references now require a strictly later fetch ordinal plus distinct
acquisition and snapshot identities, with adversarial conflation tests. No
frozen value or substantive evidence semantic changed.

Post-Review 002 same-class remediation now rejects a section request whose drug
is foreign to the report scope, exposes an exact `validate_against` comparator
to the existing `M1BResearchRequestV1` envelope across request ID, scope,
source set, and exact source-request array without adding a schema field, and
tests actual report acquisition totals one through eight plus ninth rejection.
The implementation and fresh offline validation completed before the Review
003 failure recorded below.

The Owner-authorized Review 003 remediation adds no serialized field, schema,
or public concept. It uses existing models to validate exact trusted
`AcquisitionOutcomeRef`/`SourceOutcome` pairs and a trusted selection decision,
then closes retained-response discovery-snapshot and locator
selected-candidate chains.

Post-Review 004 remediation closes exact request-owned selection authority
without a schema or public-concept change: the decision must equal the
authoritative candidate SETID and highest numeric SPL version; ephemeral tuples
of existing request/ref/outcome models and request/decision pairs must match
their exact request-owned canonical unions; pinned selection must equal its
exact pin; and a cross-request evidence-bundle swap rejects.

Bounded remediation restored the existing `SourcePlanEntry` to its exact M1A
schema and PubMed-only selected rule after full validation detected an OpenAPI
component delta. The M1B planning row now uses the distinct additive
`M1BSourcePlanEntryV1`, so approved M1B behavior does not mutate M1A.

No connector, XML/ZIP parser, ingestion, persistence, API, tool, composition,
migration, dependency, or network execution was added.

## Files in this implementation node

- `.delivery/M1B-DM-001.md`
- `docs/PRD.md`
- `docs/ARCHITECTURE.md`
- `docs/DATA_SOURCES.md`
- `docs/SECURITY.md`
- `docs/EVALUATION_PLAN.md`
- `docs/TRACEABILITY_MATRIX.md`
- `docs/decisions/ADR-011-m1b-dailymed-contracts.md`
- `src/medevidence/domain/__init__.py`
- `src/medevidence/domain/identifiers.py`
- `src/medevidence/domain/sources.py`
- `src/medevidence/domain/scope.py`
- `src/medevidence/domain/reports.py`
- `src/medevidence/domain/claims.py`
- `tests/unit/domain/test_source_outcomes.py`
- `tests/unit/domain/test_scope.py`
- `tests/unit/domain/test_reports.py`
- `tests/unit/domain/test_provenance.py`

Independent Reviews 001–006 are reviewer-owned immutable failure evidence and
are not implementation output.

## Candidate validation evidence

Focused deterministic gate:

```text
uv run --locked --no-sync pytest tests/unit/domain/test_source_outcomes.py tests/unit/domain/test_scope.py tests/unit/domain/test_reports.py tests/unit/domain/test_provenance.py --disable-socket
255 passed in 0.37s
```

The first final-remediation focused run found one incomplete local candidate
fixture and the second found a datetime test input that bypassed strict typing.
Both were mechanical test corrections; the third focused run passed without
changing frozen semantics.

Additional node-local checks executed on the implementation slice:

```text
uv run --locked --no-sync ruff check .
All checks passed!

uv run --locked --no-sync ruff format --check .
67 files already formatted

uv run --locked --no-sync mypy src
Success: no issues found in 34 source files
```

M1A OpenAPI regression and full offline gate after bounded remediation:

```text
uv run --locked --no-sync pytest tests/unit/domain/test_source_outcomes.py tests/unit/domain/test_scope.py tests/unit/domain/test_reports.py tests/unit/domain/test_provenance.py tests/contract/api/test_openapi.py --disable-socket
257 passed in 0.59s

uv run --locked --no-sync pytest tests/unit tests/contract --disable-socket --cov=medevidence --cov-report=term-missing --cov-report=xml
871 passed, 2 warnings in 5.11s; total coverage 79%; coverage.xml written
```

The two expected full-gate warnings are the existing Starlette TestClient
deprecation and the deliberate socket-block assertion. Green tests do not
override the two reproducible P1 counterexamples from independent review.
Terminal audit and final candidate identity were not produced.

Fresh authoritative root evidence for the Review 006 candidate:

```text
Focused gate: 319 passed in 0.74s
Combined DailyMed/OpenAPI gate: 321 passed in 1.16s
uv run --locked --no-sync ruff check .: All checks passed!
uv run --locked --no-sync ruff format --check .: 67 files already formatted
uv run --locked --no-sync mypy --no-incremental src: Success, 34 source files
uv run --locked --no-sync pytest tests/unit tests/contract --disable-socket --cov=medevidence --cov-report=term-missing --cov-report=xml: 935 passed, 2 warnings; total coverage 80%; 9.60s
git diff --check: passed
```

The two warnings remain the existing Starlette TestClient deprecation and the
deliberate socket-block assertion.

Fresh authoritative root evidence after Review 008 remediation:

```text
Focused gate: 301 passed in 0.46s
Combined DailyMed/OpenAPI gate: 335 passed in 0.74s
uv run --locked --no-sync ruff check .: All checks passed!
uv run --locked --no-sync ruff format --check .: 67 files already formatted
uv run --locked --no-sync mypy --no-incremental src: Success, 34 source files
uv run --locked --no-sync pytest tests/unit tests/contract --disable-socket --cov=medevidence --cov-report=term-missing --cov-report=xml: 949 passed, 2 warnings; total coverage 80%; 7.33s
git diff --check and exact 26-path scope check, including Review 008: passed
```

The two warnings remain the existing Starlette TestClient deprecation and the
deliberate socket-block assertion.

Fresh authoritative root evidence after Review 007 remediation:

```text
Focused gate: 293 passed in 0.46s
Combined DailyMed/OpenAPI gate: 327 passed in 0.70s
uv run --locked --no-sync ruff check .: All checks passed!
uv run --locked --no-sync ruff format --check .: 67 files already formatted
uv run --locked --no-sync mypy --no-incremental src: Success, 34 source files
uv run --locked --no-sync pytest tests/unit tests/contract --disable-socket --cov=medevidence --cov-report=term-missing --cov-report=xml: 941 passed, 2 warnings; total coverage 80%; 6.38s
git diff --check and exact 25-path scope check, including Review 007: passed
```

The two warnings remain the existing Starlette TestClient deprecation and the
deliberate socket-block assertion.

Fresh authoritative root evidence after Review 006 remediation:

```text
Focused gate: 291 passed in 0.67s
Combined DailyMed/OpenAPI gate: 325 passed in 1.14s
uv run --locked --no-sync ruff check .: All checks passed!
uv run --locked --no-sync ruff format --check .: 67 files already formatted
uv run --locked --no-sync mypy --no-incremental src: Success, 34 source files
uv run --locked --no-sync pytest tests/unit tests/contract --disable-socket --cov=medevidence --cov-report=term-missing --cov-report=xml: 939 passed, 2 warnings; total coverage 80%; 9.68s
git diff --check and exact 24-path scope check, including Review 006: passed
```

The two warnings remain the existing Starlette TestClient deprecation and the
deliberate socket-block assertion.

Review 002 binds candidate manifest
`f8c9cb5b13a93d4c15847855785afac80d16e0332462a9b0734e9cb576769ffe`
and returned `FAIL — P0 0 / P1 1 / P2 1`. It verified both Review 001 closures,
then reproduced a foreign-drug section request accepted by the report and found
the mandatory one-through-eight/ninth acquisition-cardinality evidence absent.
Exact counterexamples and acceptance criteria are preserved in Independent
Review 002.

Review 003 binds exact 20-path candidate manifest
`06b77e138ed4f87b2ddc749ef8eb67fd214b61512714f56b95e881afbbeeb6b3`
and returned `FAIL — P0 0 / P1 2 / P2 0`. It verified all Review 001 and Review
002 closures, then found that report references accept foreign acquisition,
intent, snapshot, and source-outcome identities because internal joins use only
source/query, and that retained-response/locator comparison omits discovery
snapshot and selected-candidate bindings. Exact reproducers and mechanical
acceptance criteria are preserved in Independent Review 003.

Review 004 binds canonical ordinal 21-path manifest
`e4f3ec8e43e2292ffe1c9c6206892f61d7111eb5c27392a21022122b14b5819e`
and CurrentCulture alias
`88574fcb86094047cb17c366bc8d6e280e2b476a7e0210b8bd57ac2fc8d98aaf`.
It returned `FAIL — P0 0 / P1 1 / P2 0` after accepting three manifestations
of one same-class defect: selected decision/candidate scalar drift, pinned
identity mismatch, and a two-drug cross-request evidence swap. Exact
counterexamples and acceptance criteria are preserved in Independent Review
004.

Review 005 binds exact 22-path manifest
`420cfd5a5ec52a30d53dee54d5bac2cfff2a11c0b450e03031434d4ea1881bca`.
It accepted direct and end-to-end foreign decision discovery/candidate-set
fields after recomputing the decision ID, and locator comparator drift across
decision label identity and retained stable identity. Exact counterexamples and
existing-field acceptance criteria are preserved in Independent Review 005.

Review 006 binds exact candidate manifest
`75416c4fb6a3df9bbcf40783bcc4aab9f12e3d3c8df9118fdaebfe7f756dbeef`
and returned `FAIL — P0 0 / P1 1 / P2 0`. In a two-request report it accepted
one-field cross-request substitution of section 2's `acquisition_id`,
`snapshot_id`, or `source_outcome_id` with section 1's value after rebuilding
the report and trusted tuples from the forged references/outcomes. Exact
reproducer output and the existing-model global-uniqueness acceptance criteria
are preserved in Independent Review 006.

Review 007 binds exact candidate manifest
`9a1ad8c2d2850c2b5ffcff67d5e19017beee740eb183c6b222270d1bdee258ca`
and returned `FAIL — P0 0 / P1 1 / P2 0`. The standalone exported
`DailyMedTrustPath.model_validate` accepted an arbitrary `/unfrozen/evil` row,
a discovery row with `allowed_query_keys=('url',)`, and a getFile row with
`exact_query=(('type', 'pdf'),)`. The parent policy rejected those rows, but
that does not close the standalone accepted construction boundary. Exact
counterexamples and acceptance criteria are preserved in Independent Review
007.

Review 008 binds exact candidate manifest
`939e99998c63dfe3ae664aa5ef6e265bc28e0e2787ea7cb73a32002dfb29e93e`
and returned `FAIL — P0 0 / P1 2 / P2 0`. Existing security-policy instances
accepted drift at counts TrustPath `0/5`, Redirect `5/5`, Transport `17/19`,
Connector `10/15`, XML `35/36`, and ZIP `25/28`, including
`authorizes_network_io=true`, `max_attempts=99`, `external_io=true`, and
`filesystem_extraction=true`. Standalone LOINC rows accepted arbitrary alias,
evil URL, and mixed-row values, while an existing oracle instance accepted
`7/8` tested drifts. Exact counterexamples and mechanical acceptance criteria
are preserved in Independent Review 008.

## Network and dependency evidence

- Medical-source network requests: none.
- Other network requests by this node: none.
- Runtime validation hosts: `[]`.
- DailyMed connector trust metadata: descriptive/non-authorizing only.
- Dependencies added or upgraded: none.
- Medical-source data, corpus files, or real patient data accessed: none.

## Review and Git state

- Independent review history: initial `FAIL`, `P0/P1/P2 = 0/4/1`; final
  remediation 3/3 Review 001 `FAIL`, `P0/P1/P2 = 0/2/0`; extra-remediation
  Review 002 `FAIL`, `P0/P1/P2 = 0/1/1`; Review 003 `FAIL`,
  `P0/P1/P2 = 0/2/0`; Review 004 `FAIL`, `P0/P1/P2 = 0/1/0`; Review 005
  `FAIL`, `P0/P1/P2 = 0/2/0`; Review 006 `FAIL`,
  `P0/P1/P2 = 0/1/0`; Review 007 `FAIL`, `P0/P1/P2 = 0/1/0`; Review 008
  `FAIL`, `P0/P1/P2 = 0/2/0`.
- Validation finding: the first full gate found the M1A `SourcePlanEntry`
  OpenAPI enum delta; bounded mechanical remediation introduced the distinct
  M1B plan model and the fresh full gate passed.
- Historical Review 001 remains immutable evidence of the two findings and
  their acceptance criteria.
- Owner-authorized extra remediation: Review 002 verified the prior closures,
  then found same-class P1-02 foreign-request binding work and dependent P2
  cardinality evidence. Both were implemented with fresh validation and
  verified closed by Review 003.
- Review 003 finding set: absent exact acquisition/outcome-reference comparator
  and incomplete retained-response/locator discovery-snapshot and
  selected-candidate bindings.
- Review 003 remediation authority: Owner authorized exactly one new cycle
  limited to P1-01/P1-02; both are implemented without schema/public-field or
  concept expansion and verified closed by Review 004.
- Review 004 finding: exact request-owned candidate/decision/evidence binding
  admits decision scalar drift, pinned mismatch, and cross-request swaps.
- Remediation: same-cycle batch mechanical remediation implemented under the
  explicit do-not-stop same-class clause; Review 005 found two further
  same-class exact-binding omissions, now implemented and verified closed by
  Review 006. Review 006 found global cross-request reuse of acquisition,
  snapshot, and source-outcome IDs. Same-class remediation now rejects global
  duplicates of all three while preserving valid acquisition-intent reuse;
  fresh root validation passed and Review 007 verified this closure. Review 007
  found the standalone exported trust-path construction weakness; same
  Owner-authorized security P1-01 remediation now enforces exact standalone
  membership in the frozen six rows while preserving parent behavior; fresh
  root validation passed and Review 008 verified this direct-construction
  closure. Review 008 found existing-instance policy bypasses and standalone
  LOINC/oracle drift. Same frozen mechanical remediation now revalidates all six
  security instance types and exact LOINC row/oracle instances; fresh root
  validation passes and Review 009 is pending.
- Terminal evidence audit: pending fresh Review 009; not run.
- Local commit: not performed by this implementation node.
- Push, pull request, CI, ready transition, merge, and local-main
  fast-forward: not performed by this implementation node.

## Remaining risks and manual verification

Repository-wide offline unit/contract interactions and byte-exact M1A OpenAPI
pass on the extra-remediation candidate. Review 002 recorded a reproducible
foreign-drug request-ownership P1 and an acquisition-cardinality P2 evidence
gap, both verified closed by Review 003. Two reproducible Review 003 provenance
binding defects were addressed in the newly authorized bounded cycle using
existing models and non-serialized comparators, and Review 004 verified them
closed. Review 004 then identified one same-class request-owned binding defect.
The post-Review 004 implementation addresses all three manifestations, but
Review 005 verified those closures and found two further same-class
exact-binding omissions. The post-Review 005 implementation addresses both;
Review 006 verified those closures and found global cross-request reuse of
`acquisition_id`, `snapshot_id`, and `source_outcome_id`. Same-class mechanical
remediation now enforces their global uniqueness while preserving valid
`acquisition_intent_id` reuse. Review 007 verified this closure but found that
standalone `DailyMedTrustPath` accepts rows outside the exact frozen six.
Security P1-01 remediation now closes that construction boundary without a
schema/public-concept change and preserves parent validation. Review 008
verified that direct closure but found existing-instance policy revalidation
bypasses and standalone/mutated LOINC drift. Mechanical remediation now closes
all six security-instance boundaries, nested connector drift, and exact
one-of-four LOINC row/oracle instance validation without changing frozen
values. Review 009 and terminal audit remain required before closure.
Commit, push, PR, CI, merge, local-main fast-forward, integrated verification,
and `M1B-DM-002` must not proceed. Future
connector/parser behavior is deliberately not implemented; the security
metadata cannot itself accept, parse, or fetch evidence. Commit, push, PR, CI,
merge, local-main fast-forward, integrated verification, and `M1B-DM-002` have
not run and remain gated.

## Technical questions for the Owner

1. Why can a partial discovery with deterministic resolved-equivalent matches
   still not produce `selected`?
2. Which identities and acquisition states must agree before a DailyMed
   section locator may exist?
3. Why does the frozen six-path connector allowlist not authorize a DailyMed
   network request?

## Review 009 evidence-only status

Independent Review 009 binds exact candidate manifest
`1cfb367f52576a765f7ccf5e3ef5d80053906dd7a8e4dffecb0066728350d3d4`
and returned `FAIL - P0 0 / P1 2 / P2 0`. It verified Review 008 closure with
focused `30 passed`, combined DailyMed/OpenAPI `335 passed`, full offline `949
passed`, two expected warnings, `80%` coverage in `6.50s`, and static checks
passing.

Review 009 reproduced accepted-instance bypasses in remaining closed DM-001
models: an existing candidate accepted 18/35 tested drifts, including
`body_complete=false` and `termination_reason="evil"`; stable section title/ID,
locator schema/kind/snapshot/fetch operation, and retained-response
schema/ID/media/bytes/time drift also accepted.

Same-class mechanical remediation now applies unified existing-instance
revalidation across 14 newly added closed DM-001 public/trusted model types,
preserves candidate completeness/termination values in `as_binding`, and
tests nested request/report and trusted-comparator boundaries.
Implementation-node evidence is focused `335 passed`, domain plus byte-exact
OpenAPI `372 passed`, Ruff PASS, format `8` owned files, MyPy PASS for `34`
source files, and diff check PASS.

Status remains awaiting root full offline validation, fresh complete
Independent Review 010, and terminal evidence audit. No PASS, Git lifecycle,
network execution, integration, or `M1B-DM-002` claim is made.

### Post-Review 009 root validation

Fresh authoritative root validation of the same-class remediation completed:

```text
Domain plus byte-exact OpenAPI: 372 passed in 0.76s
Ruff check: passed
Ruff format check: 67 files already formatted
MyPy --no-incremental: 34 source files passed
Full unit/contract suite: 951 passed, 2 expected warnings
Coverage: 80%
Full-suite duration: 6.64s
git diff --check: passed
Exact authorized scope: 27 paths, including Independent Review 009
```

Status remains awaiting fresh complete Independent Review 010 and terminal
evidence audit. No PASS, commit, push, PR, CI, ready transition, merge,
local-main fast-forward, network execution, integration, or `M1B-DM-002` claim
is made.

## Review 010 evidence-only status

Independent Review 010 binds exact 27-path manifest
`6955add1ad6e5f0d58517a749fb8b9f7b41fc1c384784ca8e11da8194b97e8e0`
and records `FAIL - P0 0 / P1 1 / P2 0`. Direct public
factory/projection/comparator calls still accepted drifted warning, candidate,
trusted SourceOutcome/decision, retained-response, locator, and report-context
instances. The same-class remediation reconstructs complete self and argument
data before every affected method uses it. It changes no serialized field,
schema, dependency, frozen semantic, or network authority. Fresh root
validation, Review 011, and terminal audit remain pending; no PASS or Git
lifecycle claim is made.

### Post-Review 010 root validation

Fresh validation of the public-method instance-revalidation closure completed:

```text
Domain plus byte-exact OpenAPI: 373 passed in 0.82s
Ruff check: passed
Ruff format check: 67 files already formatted
MyPy --no-incremental: 34 source files passed
Full unit/contract suite: 952 passed, 2 expected warnings
Coverage: 80%
Full-suite duration: 6.64s
git diff --check: passed
```

Status remains awaiting fresh complete Independent Review 011 and terminal
evidence audit. No PASS, commit, push, PR, CI, ready transition, merge,
local-main fast-forward, medical-source network execution, integration, or
`M1B-DM-002` claim is made.

## Review 011 evidence-only status

[Independent Review 011](../docs/reviews/M1B-DM-001-INDEPENDENT-REVIEW-011.md)
binds exact 28-path manifest
`564e352be9ad2470c58be20156036c9f66f8aa90ad9964048f085dc6d5de254b`
and records `FAIL — P0 0 / P1 2 / P2 0`. It verified the original Review 010
mutations reject, then reproduced a caller-controlled `intrinsic_only`
decision-validation bypass and invalid existing `SourceOutcome` acceptance at
the public classifier and report-construction boundary. Both are same-class
mechanical accepted-instance manifestations covered by the Owner's batch
authority. Remediation and fresh Review 012 are in progress; terminal audit
and all Git/PR/CI/merge/integration steps remain prohibited.

### Post-Review 011 remediation and root validation

The same-class closure removes the public intrinsic-only decision bypass,
requires complete trusted discovery context throughout warning, retained,
locator, and report comparison, reconstructs outcomes before DailyMed
classification, and reconstructs every nested report outcome at factory,
mapping, and existing-instance validation boundaries.

```text
Implementation-focused tests: 281 passed
Domain plus byte-exact OpenAPI: 380 passed in 0.86s
Ruff check: passed
Ruff format check: 67 files already formatted
MyPy --no-incremental: 34 source files passed
Full unit/contract suite: 959 passed, 2 expected warnings
Coverage: 80%
Full-suite duration: 6.85s
git diff --check: passed
```

Fresh complete Review 012 and terminal audit remain pending. No PASS, Git/PR/
CI/merge, network, integration, or DM-002 claim is made.

## Independent Review 016 PASS

[Independent Review 016](../docs/reviews/M1B-DM-001-INDEPENDENT-REVIEW-016.md)
binds the exact pre-finalization 33-path manifest
`567f4663669759a82fc67ccf25419a443b6f2e200e5e5a36226b15c81549d700`
and records `PASS — P0 0 / P1 0 / P2 0`. All Reviews 001–015 closures,
authorized scope, offline gates, M1A/OpenAPI compatibility, and zero-network
boundary were independently verified. The current evidence-finalized bytes now
require exact rebind and terminal audit. No commit, push, PR, CI, merge,
integration, medical-source network, or DM-002 claim is made.

## Terminal Audit 001 PASS

[Terminal Audit 001](../docs/reviews/M1B-DM-001-TERMINAL-AUDIT-001.md) binds the
exact pre-audit-record 34-path manifest
`8b0781a741163703467d7c96e732bee24c3854cdacde26405de886b6e1364405`
and records `PASS — P0 0 / P1 0 / P2 0`. The audit independently verified the
Review 016 binding, immutable review history, tests/static/lock evidence,
authorized scope, exact DailyMed safety/provenance semantics, and zero network.
This evidence record and status update require one final-byte rebind. No Git
lifecycle step has run yet.

## Review 015 evidence-only status

[Independent Review 015](../docs/reviews/M1B-DM-001-INDEPENDENT-REVIEW-015.md)
binds exact 32-path manifest
`9f71d93bf5710043697edfd848dc0a4d7bbb4232729edbcc3a939395f44bcd64`
and records `FAIL — P0 0 / P1 0 / P2 1`. Runtime discovery/fetch separation
is closed, but the positive test helper still copied six trusted values from
the retained response rather than constructing authority independently.
Mechanical test/evidence remediation and Review 016 are in progress; audit and
Git lifecycle remain prohibited.

### Post-Review 015 remediation and root validation

The positive trusted-fetch helper now derives attempt, manifest, member, and
link from explicit fixture constants and raw artifact/hash from independent
stable-label evidence. It no longer reads retained/locator evidence to create
authority.

```text
Report-focused tests: 137 passed
Domain plus byte-exact OpenAPI: 380 passed in 0.90s
Ruff check: passed
Ruff format check: 67 files already formatted
MyPy --no-incremental: 34 source files passed
Full unit/contract suite: 959 passed, 2 expected warnings
Coverage: 80%
Full-suite duration: 7.06s
git diff --check: passed
```

Fresh complete Review 016 and terminal audit remain pending. No PASS, Git/PR/
CI/merge, network, integration, or DM-002 claim is made.

## Review 014 evidence-only status

[Independent Review 014](../docs/reviews/M1B-DM-001-INDEPENDENT-REVIEW-014.md)
binds exact 31-path manifest
`7ba32e4738a45f990b3b6f0fde6c2d34b9a1062aed8d6f638824479739f61274`
and records `FAIL — P0 0 / P1 1 / P2 0`. Exact trusted fetch values were
present, but public retained/locator comparators did not both reassert distinct
acquisition ID, distinct snapshot, and strictly later ordinal versus discovery.
This is an existing frozen relation and same-class mechanical closure.
Remediation and Review 015 are in progress; audit/Git remain prohibited.

### Post-Review 014 remediation and root validation

Both public fetch comparators now compare trusted fetch identity directly with
the authoritative discovery decision: acquisition IDs and snapshots must
differ, and fetch ordinal must be strictly greater without a `+1` rule.
Positive trusted fixture values are independent constants or stable-label
evidence rather than copied from retained/locator objects.

```text
Relational-provenance focused tests: 281 passed
Domain plus byte-exact OpenAPI: 380 passed in 0.88s
Ruff check: passed
Ruff format check: 67 files already formatted
MyPy --no-incremental: 34 source files passed
Full unit/contract suite: 959 passed, 2 expected warnings
Coverage: 80%
Full-suite duration: 7.16s
git diff --check: passed
```

Fresh complete Review 015 and terminal audit remain pending. No PASS, Git/PR/
CI/merge, network, integration, or DM-002 claim is made.

## Review 013 evidence-only status

[Independent Review 013](../docs/reviews/M1B-DM-001-INDEPENDENT-REVIEW-013.md)
binds exact 30-path manifest
`c5ac09050724eab58b489b859d6a34d9e355ecca2adfd74bc843faf72396b959`
and records `FAIL — P0 0 / P1 1 / P2 0`. It verified Review 012 closure, then
reproduced a coherently forged fetch acquisition chain accepted by the public
retained-response and locator comparators because neither required the existing
trusted fetch reference. This is same-class mechanical exact-comparator work.
Remediation and Review 014 are in progress; audit and Git lifecycle remain
prohibited.

### Post-Review 013 remediation and root validation

The report comparator now requires a canonical request-owned nonserialized
trusted fetch-evidence row using only existing typed identities: acquisition
reference, attempt, manifest, member ordinal, link, raw artifact, and raw hash.
It enforces exact union and uniqueness and passes those independently trusted
values to both retained-response and locator comparators; neither comparator
derives authority from the other evidence object.

```text
Fetch-provenance focused tests: 281 passed
Domain plus byte-exact OpenAPI: 380 passed in 0.86s
Ruff check: passed
Ruff format check: 67 files already formatted
MyPy --no-incremental: 34 source files passed
Full unit/contract suite: 959 passed, 2 expected warnings
Coverage: 80%
Full-suite duration: 6.80s
git diff --check: passed
```

Fresh complete Review 014 and terminal audit remain pending. No PASS, Git/PR/
CI/merge, network, integration, or DM-002 claim is made.

## Review 012 evidence-only status

[Independent Review 012](../docs/reviews/M1B-DM-001-INDEPENDENT-REVIEW-012.md)
binds exact 29-path manifest
`8445fb3a9c2bed48819b03c8989f4d9ef593f3d7ede874f9010b417697f1d188`
and records `FAIL — P0 0 / P1 1 / P2 0`. It verified the other Review 011
closures, then reproduced a locator comparator path that accepted forged
selection/discovery identities when callers omitted authoritative decision
context. This is a mechanically equivalent missing exact comparator covered by
the Owner's batch-remediation authority. Remediation and fresh Review 013 are
in progress; audit and Git lifecycle remain prohibited.

### Post-Review 012 remediation and root validation

The locator comparator now requires the complete authoritative decision,
candidate, source-outcome-identity, and discovery-manifest context and always
executes the exact decision comparator. Intrinsic report construction retains
only checks provable from serialized fields and no longer presents incomplete
locator comparison as authoritative validation.

```text
Locator-focused tests: 137 passed
Domain plus byte-exact OpenAPI: 380 passed in 0.82s
Ruff check: passed
Ruff format check: 67 files already formatted
MyPy --no-incremental: 34 source files passed
Full unit/contract suite: 959 passed, 2 expected warnings
Coverage: 80%
Full-suite duration: 6.59s
git diff --check: passed
```

Fresh complete Review 013 and terminal audit remain pending. No PASS, Git/PR/
CI/merge, network, integration, or DM-002 claim is made.
